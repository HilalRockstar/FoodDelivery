import AdminShell from '../components/AdminShell'
import { useAdminOrders } from '../hooks/useAdminData'
import './AdminPage.css'

function AdminOrders() { const { data: orders, loading } = useAdminOrders(); const revenue = orders.reduce((sum, item) => sum + Number(item.totalAmount || 0), 0); return <AdminShell eyebrow="LIVE OPERATIONS" title="Orders" description="Monitor every order as it moves from kitchen confirmation to the customer’s door." action={<span className="admin-live"><i /> Live database</span>}><div className="admin-stats"><div className="admin-stat"><p>Total orders</p><strong>{orders.length}</strong><span>From your database</span></div><div className="admin-stat"><p>In progress</p><strong>{orders.filter((item) => !['DELIVERED', 'CANCELLED'].includes(item.status)).length}</strong><span>Active now</span></div><div className="admin-stat"><p>Delivered</p><strong>{orders.filter((item) => item.status === 'DELIVERED').length}</strong><span>Completed orders</span></div><div className="admin-stat"><p>Revenue</p><strong>₹{revenue.toFixed(0)}</strong><span>Loaded from orders</span></div></div><section className="admin-table-wrap"><div className="admin-table-toolbar"><h2>Recent orders</h2><span className="subtle">Live database records</span></div>{loading ? <p className="loading-state">Loading orders...</p> : <table className="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Restaurant</th><th>Total</th><th>Status</th></tr></thead><tbody>{orders.map((item) => <tr key={item.id}><td><strong>#{item.id}</strong></td><td>{item.user?.fullName || 'Customer'}</td><td>{item.restaurant?.name || 'Restaurant'}</td><td>₹{Number(item.totalAmount).toFixed(2)}</td><td><span className={`status-badge ${item.status === 'PREPARING' ? 'busy' : ''}`}>{String(item.status).replaceAll('_', ' ')}</span></td></tr>)}</tbody></table>}</section></AdminShell> }

export default AdminOrders